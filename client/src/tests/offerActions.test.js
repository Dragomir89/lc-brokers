import { GET_OFFERS } from '../actions/offerActionTypes';
import { getOffers } from '../actions/offerActions';
// import { async } from 'q';

test('should return correct getOffersAction', () => {

    // const data = await getOffers();
    // console.log(data);
    // expect(data()).toEqual({type:GET_OFFERS});

    // function callBack (data) {
    //     const res = getOffers();
    //     expect(res).toEqual({ type: GET_OFFERS, payload: [] });
    // }    
})